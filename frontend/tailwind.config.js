/** @type {import('tailwindcss').Config} */

// Tailwind CSS v4 — configuração CSS-first.
// Os TOKENS do Design System (cores, tipografia, radius, spacing, sombras)
// vivem EXCLUSIVAMENTE no bloco `@theme` de src/styles/index.css.
// NÃO duplicar tokens aqui. Este arquivo fica mínimo: honra a árvore do
// projeto e serve de gancho futuro (plugins/overrides) caso necessário.
export default {
  theme: {
    extend: {},
  },
}
